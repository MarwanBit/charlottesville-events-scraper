[pytest]
# Only look for tests under tests/ (never scan repo root or .venv)
testpaths = tests
# Never recurse into these (avoids slow scan of .venv, node_modules, etc.)
norecursedirs = .git .hg .venv venv env __pycache__ .terraform node_modules .pytest_cache
# Short output
addopts = -ra
